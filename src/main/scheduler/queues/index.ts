import GameStartTimerQueue from "./gameStartTimer.queue";
import turnStartTimerQueue from "./turnStartTimer.queue";

const exportObject = {
  GameStartTimerQueue,
  turnStartTimerQueue,
};
export = exportObject;
